<nav class='flex sticky top-0 h-16 p-4 text-center justify-between bg-slate-200'>
  <div class='flex items-center'>
    <div class='p-4'>
      <%= link_to 'Home', root_path, class: 'flex' %>
    </div>

    <div class='p-4'>
      <div data-controller="dropdown" class="relative">
        <button type="button" data-action="dropdown#toggle click@window->dropdown#hide" class='flex items-center'>
          <%= t 'select_part_of_speech' %>
          <svg class="w-2.5 h-2.5 ms-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 10 6">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 4 4 4-4"/>
          </svg>
        </button>

        <div
          data-dropdown-target="menu"
          class="bg-white hidden transition transform origin-top-right absolute right-0"
          data-transition-enter-from="opacity-0 scale-95"
          data-transition-enter-to="opacity-100 scale-100"
          data-transition-leave-from="opacity-100 scale-100"
          data-transition-leave-to="opacity-0 scale-95"
        >
          <%= link_to t('Articles'), articles_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
          <%= link_to t('Cases'),cases_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
          <%= link_to t('Genders'),genders_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
          <%= link_to t('Numbers'), numbers_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
          <%= link_to t('Personal Pronouns'), personal_pronouns_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
          <%= link_to t('Possesive Pronouns'), possessive_pronouns_path, :'data-action' => 'dropdown#toggle', class: 'flex' %>
        </div>
      </div>
    </div>
  </div>
  
  <div class='flex'>
    <div class='p-2'>
      <div data-controller="dropdown" class="relative">
        <button type="button" data-action="dropdown#toggle click@window->dropdown#hide" class='flex items-center'>
          <%= t(locale, scope: 'locales') %>
          <svg class="w-2.5 h-2.5 ms-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 10 6">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 1 4 4 4-4"/>
          </svg>
        </button>

        <div
          data-dropdown-target="menu"
          class="hidden transition transform origin-top-right absolute right-0"
          data-transition-enter-from="opacity-0 scale-95"
          data-transition-enter-to="opacity-100 scale-100"
          data-transition-leave-from="opacity-100 scale-100"
          data-transition-leave-to="opacity-0 scale-95"
        >
          <ul class='bg-white px-4'>
            <% I18n. available_locales.each do |locale| %>
              <li>
                <% if I18n.locale != locale %>
                  <%= link_to t(locale, scope: 'locales'), url_for(locale: locale), :class => 'hover:bg-sky-200' %>
                <% end %>
              </li>
            <% end %> 
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>