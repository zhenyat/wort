<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= "#{t('header.new', model: @verb.class.model_name.human)}" %></h1>

  <%= render "form", verb: @verb %>

  <%= link_to t('button.back'), verbs_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
